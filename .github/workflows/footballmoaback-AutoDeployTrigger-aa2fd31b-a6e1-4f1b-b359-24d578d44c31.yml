name: Trigger auto deployment for footballmoaback

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/footballmoaback-AutoDeployTrigger-aa2fd31b-a6e1-4f1b-b359-24d578d44c31.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.FOOTBALLMOABACK_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.FOOTBALLMOABACK_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.FOOTBALLMOABACK_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: .docker.io
          registryUsername: ${{ secrets.DOCKERHUB_USERNAME }}
          registryPassword: ${{ secrets.DOCKERHUB_PASSWORD }}
          containerAppName: footballmoaback
          resourceGroup: ggang
          imageToBuild: index.docker.io/v1/kangbyoenggyu/footballmoaback:${{ github.sha }}
          


